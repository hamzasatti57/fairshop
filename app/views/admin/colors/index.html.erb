<div class="row">
  <div class="col-lg-8 offset-2">
    <div class="card">
      <div class="card-header custom-card-header">
        <i class="fa fa-align-justify"></i> Listing all Colors</div>
      <div class="card-body">
        <table class="table table-responsive-sm">
          <thead>
          <tr>
            <th>Title</th>
            <th>Code</th>
            <th>Actions</th>
          </tr>
          </thead>
          <tbody>
          <% @colors.each do |color| %>
            <tr>
              <td><%= color.title %></td>
              <td><%= truncate(color.code, length: 50) %></td>
              <td><%= link_to "Edit",  edit_admin_color_path(color), class: "btn btn-xs custom-table-edit-btn btn-primary" %>
              <%= link_to "Delete", admin_color_path(color), method: :delete , data: {confirm: "Are you sure?"} , class: "btn btn-xs btn-danger custom-table-delt-btn" %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
